# apps/navigator-experimental — CLAUDE.md

App: `navigator-experimental` (package name in `package.json`)
Track: **EXPERIMENTAL** — proving ground for features before promotion to generic.
([ADR-006](../../docs/adr/ADR-006-three-track-delivery-model.md))

Platform-wide conventions: see root [`CLAUDE.md`](../../CLAUDE.md).
Per-package rules: [`packages/ui/CLAUDE.md`](../../packages/ui/CLAUDE.md),
[`packages/navigator-data/CLAUDE.md`](../../packages/navigator-data/CLAUDE.md),
[`packages/features/CLAUDE.md`](../../packages/features/CLAUDE.md).

---

## Track rules

- **Allowed feature stability levels: `experimental`, `candidate`, and `stable`.**
  This app is the seeding ground for the initial set of stability-flagged features
  (HZN-1.10). All three tiers are importable here.
- **Purpose: validate features before promotion to generic.** When a feature in
  this app is deemed ready for production, open a promotion PR per the workflow
  in [`packages/features/CLAUDE.md`](../../packages/features/CLAUDE.md).
- **Never a public URL.** This app runs on an internal, auth-gated preview
  environment. No `navigator-experimental.*` artifact is hosted publicly.
  Prospects see in-flight UX in a controlled setting.
- **Not a permanent home.** Features should not live at `"experimental"` forever.
  Stale features that are never promoted should be removed or explicitly
  downscoped.

---

## Dev commands

```
pnpm dev      # starts Vite dev server on port 5174
pnpm build    # tsc -b && vite build (experimental build)
pnpm preview  # serves the experimental build locally
```

Note: dev port is `5174` (generic uses `5173`) to allow both apps to run
simultaneously.

---

## File structure

```
apps/navigator-experimental/src/
  main.tsx           # React root, router setup
  routeTree.gen.ts   # auto-generated by TanStack Router plugin — do not edit
  routes/
    __root.tsx       # root layout route
    index.tsx        # root index route
```

Routes are file-based (TanStack Router, ADR-005). `routeTree.gen.ts` is
generated automatically — never edit it manually.

---

## Promotion workflow

To move a feature from this track to the generic track:

1. Confirm the feature's `x-stability` is ready to advance
   (run the dependency graph check — see
   [`packages/features/CLAUDE.md`](../../packages/features/CLAUDE.md)).
2. Open a PR that flips `x-stability` in the feature's `package.json`.
3. Add the feature to `apps/navigator`'s import allowlist
   (once HZN-1.9 ESLint/bundle-audit enforcement is wired).
4. Code review is the gate. No code moves, no fork drift.

Reference: [ADR-006](../../docs/adr/ADR-006-three-track-delivery-model.md),
[`packages/features/CLAUDE.md`](../../packages/features/CLAUDE.md).
