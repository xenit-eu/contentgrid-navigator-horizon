/** Entity names as they appear in the test-environment data model's sidebar. */
export const ENTITY = {
  ALL_ATTRIBUTE: "All attribute",
  EMPLOYEE: "Employee",
  ORDER: "Order",
  READ_ALLOWED: "Read allowed",
  NOT_ALLOWED: "Not allowed",
  CREATE_ALLOWED: "Create allowed",
  MANY_RELATION: "Many relation",
} as const;
