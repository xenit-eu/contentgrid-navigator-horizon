/**
 * Placeholder spec — ACC-2864 / HZN-2.5
 *
 * This file exists so Playwright finds at least one test and exits 0.
 * Real E2E tests will be added in follow-up tickets once the webServer
 * is connected to a ContentGrid test environment.
 */
import { test } from "@playwright/test";

test.skip("placeholder — no E2E tests yet", () => {
  // intentionally empty
});
