export function ExperimentalSandbox() {
  return (
    <div className="rounded-md border border-[var(--amber)] bg-muted px-4 py-3 text-sm text-[var(--amber)]">
      Experimental sandbox — not for production use.
    </div>
  );
}
